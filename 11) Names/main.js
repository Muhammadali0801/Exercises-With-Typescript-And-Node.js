"use strict";
/*Names: Store the names of a few of your friends in a array called names. Print each person’s name by accessing
each element in the list, one at a time.*/
let names = ["Ali", "hamza", "Azmat", "Asad"];
// Printing each person's name by accessing each element in the list
console.log("Printing names:");
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
