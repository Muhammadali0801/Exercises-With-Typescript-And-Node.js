"use strict";
/*Famous Quote: Find a quote from a famous person you admire. Print the quote and the name of its author. Your output
 should look something like the following, including the quotation marks:*/
// Storing the famous quote in a variable
let famous_quote = "I never dreamed about success, I worked for it.";
// Storing the author's name in a variable
let author_name = " Estee Lauder";
// Composing the message
let message = ` ${author_name} once said, "${famous_quote}"`;
// Printing the message
console.log(message);
